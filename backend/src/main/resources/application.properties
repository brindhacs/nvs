# # spring.application.name=nvs

# # # Database
# # spring.datasource.url=jdbc:mysql://localhost:3306/nvsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# # spring.datasource.username=root
# # spring.datasource.password=ROOT
# # spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # # JPA / Hibernate
# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # # Security (optional test credentials)
# # spring.security.user.name=admin
# # spring.security.user.password=admin123




# spring.application.name=nvs

# # ==============================
# # Database Configuration
# # ==============================
# spring.datasource.url=jdbc:mysql://localhost:3306/nvsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=ROOT
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # ==============================
# # Hibernate / JPA
# # ==============================
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # ==============================
# # Spring Security (Temporary Admin Login)
# # ⚠️ You should remove this after JWT is added
# # ==============================
# spring.security.user.name=admin
# spring.security.user.password=admin123

# # ==============================
# # JWT Authentication Settings
# # ==============================
# # Secret Key — use a long random string
# app.jwt.secret=THIS_IS_A_DEMO_SECRET_CHANGE_IT_1234567890_ABCDEFG

# # Token expiry (1 day = 86400000 ms)
# app.jwt.expiration-ms=86400000


# existing database settings...
spring.application.name=nvs
spring.datasource.url=jdbc:mysql://localhost:3306/nvsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ROOT
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT config
app.jwt.secret=Brindha@88

# 24 hours in milliseconds
app.jwt.expiration-ms=86400000

# optional
# spring.main.allow-circular-references=false
